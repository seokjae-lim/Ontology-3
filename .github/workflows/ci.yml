name: ci
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: u_oip
          POSTGRES_USER: u_oip
          POSTGRES_PASSWORD: u_oip
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U u_oip -d u_oip"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install -r backend/requirements.txt
      - run: pytest -q
        working-directory: backend
